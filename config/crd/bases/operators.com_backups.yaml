---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backups.operators.com
spec:
  group: operators.com
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup
            properties:
              resources:
                description: Resources is a list of Kubernetes resources to include
                  in the backup
                items:
                  description: Resource defines a Kubernetes resource with its name,
                    group and version
                  properties:
                    group:
                      description: |-
                        Group is the API group the resource belongs to.
                        For resources in the core API group (e.g., "pods", "services"), leave this empty.
                      type: string
                    name:
                      description: Name is the name of the Kubernetes resource
                      type: string
                    version:
                      description: Version is the API version of the resource.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              storageBucket:
                description: StorageBucket is the name of the MinIO bucket to store
                  backups.
                type: string
            required:
            - resources
            - storageBucket
            type: object
          status:
            description: BackupStatus defines the observed state of Backup
            properties:
              backupFileName:
                description: BackupFileName is the name of the backup file in MinIO.
                type: string
              completionTime:
                description: CompletionTime is the time at which the backup was completed.
                format: date-time
                type: string
              error:
                description: Error message if the backup failed.
                type: string
              startTime:
                description: StartTime is the time at which the backup was started.
                format: date-time
                type: string
              status:
                description: Status of the backup.  Can be "Completed", "Failed",
                  or "InProgress".
                enum:
                - Completed
                - Failed
                - InProgress
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
